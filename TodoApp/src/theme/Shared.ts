import {DefaultTheme} from '@react-navigation/native';

const Shared = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primarycolor: '#047ABC',
    secondaryColor: '#3286E7',
    backgroundColor: '#FFFFFF',
    backgroundColorBrightness: '#000000',
  },
};

export default Shared;
